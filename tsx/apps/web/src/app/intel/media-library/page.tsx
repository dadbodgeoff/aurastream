'use client';

/**
 * Creator Media Library Page
 * 
 * Unified storage for all user-uploaded assets that can be
 * injected into any generation prompt.
 * 
 * @module app/intel/media-library/page
 */

import { MediaLibraryPage } from '@/components/media-library';

export default function MediaLibraryRoute() {
  return <MediaLibraryPage />;
}
